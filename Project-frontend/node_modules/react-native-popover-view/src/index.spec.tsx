import Popover from '.';
import React, { ReactElement } from 'react';
import { Text } from 'react-native';

export default function PopoverTest(): ReactElement {
  return (
    <Popover>
      <Text>Test</Text>
    </Popover>
  );
}
